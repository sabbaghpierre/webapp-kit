name: webapp_tui
description: Nocterm TUI frontend for the webapp-kit isolated-webapps toolkit.
version: 0.0.1

environment:
  sdk: ^3.13.0
dependencies:
  nocterm: ^0.9.0
dev_dependencies:
  test: ^1.32.0
